import { SemesterDisplayNamePipe } from './semester-display-name-pipe';

describe('SemesterDisplayNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SemesterDisplayNamePipe();
    expect(pipe).toBeTruthy();
  });
});
