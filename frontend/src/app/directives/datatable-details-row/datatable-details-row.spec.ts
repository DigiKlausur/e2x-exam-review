import { DatatableDetailsRow } from './datatable-details-row';

describe('DatatableDetailsRow', () => {
  it('should create an instance', () => {
    const directive = new DatatableDetailsRow();
    expect(directive).toBeTruthy();
  });
});
