import { ShowInvalid } from './show-invalid';

describe('ShowInvalid', () => {
  it('should create an instance', () => {
    const directive = new ShowInvalid();
    expect(directive).toBeTruthy();
  });
});
