import { CustomInput } from './custom-input';

describe('CustomInput', () => {
  it('should create an instance', () => {
    const directive = new CustomInput();
    expect(directive).toBeTruthy();
  });
});
